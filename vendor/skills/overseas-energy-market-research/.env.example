# ===== 环境变量示例（复制为 .env 或写入系统环境变量）=====

# ---- AnySearch API Key（可选，推荐）----
# 无 key 可用匿名访问（较低限流）；注册免费 key：https://anysearch.com/console/api-keys
# 优先级：--api_key > .env > 环境变量 > 匿名
# ANYSEARCH_API_KEY=as_sk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ---- EWO 生图 API（可选，PPT 封面/插图）----
# 本地代理默认端点与 token；EWO 不可用时封面自动降级为咨询排版 + 手写 SVG
# EWO_ORIGIN=http://127.0.0.1:18799
# EWO_KEY=xxxxxxxx  # 即 ~/.ewo/.habitat-local-proxy-token 的内容

# ---- Python 解释器覆盖（可选）----
# 部分构建步骤需要 matplotlib/docx/openpyxl/pptx 等包；默认探测 PATH 上的 Python，
# 需要显式指定时设置：
# OVERSEAS_RESEARCH_PYTHON=C:\Python310\python.exe

# ---- 代理（可选，Windows 本机惯例）----
# HTTP_PROXY=http://127.0.0.1:7897
# HTTPS_PROXY=http://127.0.0.1:7897
# NO_PROXY=127.0.0.1,localhost
